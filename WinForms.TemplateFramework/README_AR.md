> [View in English](README.md) |

---

# 🏗️ نماذج ويندوز: نظام "المحرر العام" عن طريق الوراثة البصرية
يوضح هذا المستودع كيفية تنفيذ الوراثة البصرية (Visual Inheritance) في نماذج ويندوز (.NET). حيث ينشئ نظامًا موحدًا "للمحرر" يتكون من نموذج أساسي (قالب) ونموذج فرعي (تنفيذ) لتقليل تكرار التعليمات البرمجية وفرض اتساق واجهة المستخدم.

## 📖 السيناريو
نقوم ببناء نظام إدخال بيانات حيث تحتاج كل نافذة إلى مظهر وشكل قياسي:

- **النموذج الأساسي (BaseEditorForm)**: يحدد الهيكل (رأس، أزرار حفظ/إلغاء) ويتعامل مع المنطق القياسي (إغلاق النافذة).
- **النموذج الفرعي (UserEntryForm)**: يرث الهيكل، يضيف حقول إدخال محددة (مربع الاسم)، ويتجاوز منطق "الحفظ" لإجراء التحقق من الصحة وعمليات قاعدة البيانات.

## 🚀 البدء السريع

### المتطلبات الأساسية
- Visual Studio 2019 أو أحدث.
- دعم .NET Framework أو .NET (Core) لنماذج ويندوز.

### التثبيت والاستخدام
1. استنسخ المستودع.
2. افتح الحل في Visual Studio.
3. **هام جدًا**: ابنِ الحل (Ctrl + Shift + B) قبل فتح المصمم. يتطلب Visual Studio تجميع النموذج الأساسي إلى DLL لعرض النموذج الفرعي.
4. شغّل التطبيق (F5). تم ضبط Program.cs لبدء تشغيل UserEntryForm.

## 🛠️ الهندسة المعمارية والتصميم

### 1. النموذج الأساسي (BaseEditorForm)
القالب الذي يحدد الهيكل القياسي.

**عناصر التحكم في واجهة المستخدم:**
- لوحة علوية تحتوي على عنوان.
- أزرار "حفظ" و "إلغاء" مثبتة في الأسفل يمين.

**التكوين الرئيسي (المعدِّلات):**
- `btnCancel`: خاص (مقفل). النماذج الفرعية لا يمكنها نقل أو حذف هذا الزر.
- `btnSave` و `lblTitle`: محمي (مفتوح). النماذج الفرعية يمكنها تغيير النص أو الموقع.

**المنطق الأساسي**
يتعامل النموذج الأساسي مع حدث الإغلاق لكنه يترك منطق الحفظ افتراضيًا (virtual) ليعرفه النموذج الفرعي.

```csharp
public partial class BaseEditorForm : Form
{
    public BaseEditorForm()
    {
        InitializeComponent();
    }

    // منطق قياسي: يُغلق النافذة دائمًا. ليس افتراضيًا.
    private void btnCancel_Click(object sender, EventArgs e)
    {
        this.Close();
    }

    // منطق التشغيل: يستدعي الأسلوب الافتراضي.
    private void btnSave_Click(object sender, EventArgs e)
    {
        PerformSave();
    }

    // VIRTUAL: يجب على النموذج الفرعي تجاوز هذا الأسلوب لإضافة الوظيفة.
    protected virtual void PerformSave()
    {
        MessageBox.Show("تم استدعاء وظيفة الحفظ الأساسية.");
    }
}
```

### 2. النموذج الفرعي (UserEntryForm)
التنفيذ المحدد لإنشاء مستخدم.

**الوراثة:** معرفة كـ `public partial class UserEntryForm : BaseEditorForm`

**التخصيص:**
- تغيير العنوان إلى "إنشاء مستخدم جديد".
- إضافة تسمية ومربع نص لإدخال الاسم.

**المنطق:** يتجاوز `PerformSave()` للتحقق من صحة المدخلات.

```csharp
protected override void PerformSave()
{
    // 1. التحقق من الصحة المحدد
    if (string.IsNullOrEmpty(txtName.Text))
    {
        MessageBox.Show("يرجى إدخال الاسم أولاً!", "خطأ في التحقق من الصحة");
        return;
    }

    // 2. منطق الحفظ المحدد
    MessageBox.Show($"تم حفظ المستخدم '{txtName.Text}' بنجاح.", "نجاح");
    
    // 3. (اختياري) استدعاء base.PerformSave() إذا لزم الأمر
}
```

## 📚 ورقة مرجعية للمفاهيم
استخدم هذا الجدول لفهم العلاقة بين النماذج الأساسية والفرعية.

| الكلمة المفتاحية / الإعداد | السياق | الشرح |
|----------------------------|---------|--------|
| `: BaseEditorForm` | تعريف الفئة | يخبر المترجم باستخدام النموذج المخصص كقالب بدلاً من النموذج القياسي. |
| Private (خاص) | الخصائص | مقفل. النموذج الفرعي يمكنه رؤية عنصر التحكم لكن لا يمكنه لمسه أو نقله أو تحريره. |
| Protected (محمي) | الخصائص | مفتوح. النموذج الفرعي "يمتلك" عنصر التحكم هذا فعليًا ويمكنه تعديله. |
| `virtual` | الكود الأساسي | "إليك أسلوب، لكني أسمح لأطفالي باستبدال طريقة عمله." |
| `override` | كود النموذج الفرعي | "أنا أستبدل الأسلوب الافتراضي من والدي بهذا الكود الجديد." |
| `base.Method()` | كود النموذج الفرعي | "شغّل النسخة الأصلية من هذا الأسلوب، ثم استمر في تنفيذ نسختي." |

## 💡 نصيحة احترافية: مشكلة "DesignMode"
عند استخدام الوراثة، يتم تشغيل الكود في حدث تحميل النموذج الأساسي في كل مرة تفتح فيها النموذج الفرعي في مصمم Visual Studio. إذا كان لديك اتصالات بقاعدة البيانات هناك، سيتعطل المصمم.

**الحل:** دائمًا غلّف الكود الخطير في النموذج الأساسي كما يلي:

```csharp
protected override void OnLoad(EventArgs e)
{
    if (this.DesignMode) return; // توقف هنا إذا كنت داخل مصمم Visual Studio!

    // أصبح من الآمن الآن تشغيل كود قاعدة البيانات...
    base.OnLoad(e);
}
```
